# =============================================================================
#  Application Properties for ARMS
#  Project: arms
#  Technology: Java, Spring Boot, Maven
#  Version: 2.0.0
#  Author: Mathew Bass
#  ---------------------------------------------------------------------------
#  This file contains configuration properties for the ARMS application.
#  Edit values as needed for your local or deployment environment.
# =============================================================================
spring.application.name=arms
server.address=127.0.0.1
server.port=3737

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

#Use H2 dialect when running against H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# enable H2 console for debugging
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Properties
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true